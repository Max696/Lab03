
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(bitacora => bitacora)
        </th>
        <th></th>
    </tr>

    @foreach (var item in )
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.noPartido)
            </td>
            
        </tr>
    }

</table>
